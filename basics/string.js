const name="git"
const title="hub"

console.log(`my name is ${name} and my title is ${title}.so my full name is ${name} ${title}`);
console.log(name[0]);

const gamename=new String('github')
console.log(gamename);

// other methods in string
